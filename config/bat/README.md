# Config notes

## `syntaxes/PHP.sublime-syntax`

Workaround for PHP syntax highlighting issues with delta
(see: https://github.com/dandavison/delta/issues/162#issuecomment-625952288)

Run `bat cache --build` to enable.
